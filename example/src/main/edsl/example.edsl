globalbase {
  option package io.activise.order;
}

entity Order(orders) {
  id: String {pk};
  createdDate: DateTime;
  customerReference: String {idx};
  orderEntries: OrderEntry[];
}

entity OrderEntry(order_entries) {
  order:+ Order;
  money:- Money;
}
